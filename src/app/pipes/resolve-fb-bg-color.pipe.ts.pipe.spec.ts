import { ResolveFbBgColorPipeTsPipe } from './resolve-fb-bg-color.pipe.ts.pipe';

describe('ResolveFbBgColorPipeTsPipe', () => {
  it('create an instance', () => {
    const pipe = new ResolveFbBgColorPipeTsPipe();
    expect(pipe).toBeTruthy();
  });
});
